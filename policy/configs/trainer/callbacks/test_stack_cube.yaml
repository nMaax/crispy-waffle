defaults:
  - default.yaml
  - _self_

# Held-out evaluation rollout config shared by every `*__StackCube-v1__*__test.yaml` experiment.
rollout_evaluation:
  num_episodes: 100
  env_id: "StackCube-v1"
  obs_mode: "state"
  control_mode: "pd_ee_delta_pos"
  physx_backend: "physx_cuda"
  canonicalize: true
