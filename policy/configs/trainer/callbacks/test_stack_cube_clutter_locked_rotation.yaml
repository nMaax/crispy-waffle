defaults:
  - default.yaml
  - _self_

# Held-out evaluation rollout config for StackCubeClutterLockedRotation-v1
rollout_evaluation:
  num_episodes: 100
  env_id: "StackCubeClutterLockedRotation-v1"
  obs_mode: "state_dict"
  control_mode: "pd_ee_delta_pos"
  physx_backend: "physx_cuda"
  canonicalize: true
