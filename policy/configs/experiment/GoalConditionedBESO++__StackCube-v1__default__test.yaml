# @package _global_
defaults:
  - GoalConditionedBESO++__StackCube-v1__default__train

name: ${hydra:runtime.choices.experiment}
ckpt_path: ???

trainer:
  callbacks:
    rollout_evaluation:
      num_inference_steps: 3
      num_episodes: 100
