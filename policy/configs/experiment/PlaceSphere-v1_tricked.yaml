# @package _global_
defaults:
  - StackCube-v1_neuromotor

name: PlaceSphere-v1_tricked_run
ckpt_path: ???

trainer:
  callbacks:
    rollout_evaluation:
      adapter:
        _target_: policy.utils.adapters.place_sphere_to_stack_cube.PlaceSphereToStackCubeAdapter
      num_episodes: 4
      num_envs: 4
      video_dir: "${hydra:runtime.output_dir}/renderings/"

      env_id: "PlaceSphereWristcam-v1"
      obs_mode: "state"
      control_mode: "pd_ee_delta_pos"
      physx_backend: null
