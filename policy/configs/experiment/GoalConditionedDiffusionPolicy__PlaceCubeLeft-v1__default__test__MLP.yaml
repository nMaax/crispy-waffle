# @package _global_
defaults:
  - GoalConditionedDiffusionPolicy__StackCube-v1__default__train__MLP

name: ${hydra:runtime.choices.experiment}
ckpt_path: ???

trainer:
  callbacks:
    rollout_evaluation:
      num_episodes: 100
      env_id: "PlaceCubeLeft-v1"
      obs_mode: "state"
      control_mode: "pd_ee_delta_pos"
      physx_backend: "physx_cuda"
      canonicalize: true
