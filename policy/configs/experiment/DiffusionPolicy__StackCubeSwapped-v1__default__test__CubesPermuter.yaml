# @package _global_
defaults:
  - DiffusionPolicy__StackCube-v1__default__test

name: ${hydra:runtime.choices.experiment}
ckpt_path: ???

trainer:
  callbacks:
    rollout_evaluation:
      adapter:
        # TODO: StackCubeSwapped-v1 + CubesPermuter isn't going to return the original StackCube?
        _target_: policy.utils.adapters.CubesPermuter
        swap_env_indices: "odd"

      env_id: "StackCubeSwapped-v1"
      obs_mode: "state"
      control_mode: "pd_ee_delta_pos"
      physx_backend: "physx_cuda"
