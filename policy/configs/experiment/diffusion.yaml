# @package _global_
defaults:
    - override /algorithm: diffusion
    - override /datamodule: maniskill_datamodule

# The name of the experiment (for logging)
name: diffusion_run

datamodule:
    dataset_file: /home/massimiliano/.maniskill/demos/StackCube-v1/motionplanning/trajectory.h5
