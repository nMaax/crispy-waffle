# @package _global_
defaults:
  - GoalConditionedDiffusionPolicy__StackCube-v1__default__train

name: ${hydra:runtime.choices.experiment}
ckpt_path: ???

trainer:
  callbacks:
    rollout_evaluation:
      num_episodes: 100
