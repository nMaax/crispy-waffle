# @package _global_
defaults:
  - StackCube-v1_neuromotor

name: PlaceSphereWithCubes-v1_sanitycheck_run
ckpt_path: ???

trainer:
  callbacks:
    rollout_evaluation:
      num_episodes: 4
      num_envs: 4
      video_dir: "${hydra:runtime.output_dir}/renderings/"

      env_id: "PlaceSphereWithCubes-v1"
      obs_mode: "state"
      control_mode: "pd_ee_delta_pos"
      physx_backend: null
