# @package _global_
defaults:
  - GoalConditionedDiffusionPolicyMLP__StackCubeLockedRotation-v1__default__train
  - override /algorithm: goal_conditioned_diffusion_policy_attention_attn_pool

name: ${hydra:runtime.choices.experiment}

algorithm:
  goal_delta: input # WARN: Important! Using embed is degenerate
