# @package _global_
defaults:
  - GCDP-Obj-Attn-AttnPool__SCLR__default__train__NoRole

name: ${hydra:runtime.choices.experiment}

algorithm:
  encoder:
    embedder:
      include_feedforward: false
