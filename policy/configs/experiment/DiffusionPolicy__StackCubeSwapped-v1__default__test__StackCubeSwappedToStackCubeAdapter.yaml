# @package _global_
defaults:
  - DiffusionPolicy__StackCube-v1__default__test

name: ${hydra:runtime.choices.experiment}
ckpt_path: ???

trainer:
  callbacks:
    rollout_evaluation:
      adapter:
        _target_: policy.adapters.stack_cube_swapped_to_stack_cube.StackCubeSwappedToStackCubeAdapter
        swap_env_indices: "odd"

      env_id: "StackCubeSwapped-v1"
      obs_mode: "state"
      control_mode: "pd_ee_delta_pos"
      physx_backend: "physx_cuda"
