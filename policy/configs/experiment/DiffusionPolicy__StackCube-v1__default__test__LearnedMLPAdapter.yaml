# @package _global_
defaults:
  - DiffusionPolicy__StackCube-v1__default__test

name: ${hydra:runtime.choices.experiment}
ckpt_path: ???

trainer:
  callbacks:
    rollout_evaluation:
      adapter:
        _target_: policy.utils.adapters.learned_mlp_adapter.LearnedMLPAdapter
        ckpt_path: ???

        # TODO: Do hydra support slices natively? Without using strings?
        passthrough_mapping:
          - ["0:25", "0:25"]
