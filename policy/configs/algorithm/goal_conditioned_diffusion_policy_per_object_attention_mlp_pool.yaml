defaults:
  - goal_conditioned_diffusion_policy_per_object_attention
  - override pooling@encoder.pooling: mlp

encoder:
  pooling:
    # Must equal len(tokenizer.object_keys) (3 by default): the number of tokens per timestep
    # that MLPPooling's internal Linear must be sized for.
    tokens_per_step: 3
