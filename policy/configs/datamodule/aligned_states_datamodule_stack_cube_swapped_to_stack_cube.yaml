defaults:
  - aligned_states_datamodule

base_datamodule:
  # In fact, StackCube-v1 dataset would be fine too! As the swapped version do not modify the state, but only the action taken and the success/reward
  dataset_file: ${oc.env:HOME}/.maniskill/demos/StackCubeSwapped-v1/motionplanning/trajectory.state.pd_ee_delta_pos.physx_cpu.h5

adapter:
  _target_: policy.adapters.stack_cube_swapped_to_stack_cube_adapter.StackCubeSwappedToStackCubeAdapter
